# PostgreSQL

Development database is defined in `deploy/docker-compose.dev.yml`.

Production notes will be added later:

- separate database user
- backup policy
- migration strategy
- retention policy for audit logs and heartbeats
