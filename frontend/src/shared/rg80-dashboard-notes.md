# RG-80 Dashboard Visual Alignment Notes

This file is intentionally not imported by the application.

It documents the UI intent for this pass:

- fixed dashboard layout by default;
- widgets implemented as independent blocks;
- reference-inspired app shell with sidebar, topbar, status bar;
- mock placeholders only where backend data is not available yet.
