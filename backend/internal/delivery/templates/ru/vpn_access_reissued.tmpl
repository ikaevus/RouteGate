{{define "subject"}}Доступ к RouteGate VPN обновлён{{end}}
{{define "text"}}Ваш доступ к RouteGate VPN был обновлён.
{{if .ProfileName}}Профиль: {{.ProfileName}}
{{end}}Протокол: {{.Access.DisplayName}}

{{.Access.PrimaryAction}}
{{if .Access.URI}}
Обновлённые данные подключения:
{{.Access.URI}}
{{end}}{{if .Access.AlternativeURI}}
Обновлённая ссылка для Telegram:
{{.Access.AlternativeURI}}
{{end}}{{if .Access.ConfigText}}
Обновлённая конфигурация WireGuard:
{{.Access.ConfigText}}
{{end}}{{if .Access.SubscriptionURL}}
Подписка:
{{.Access.SubscriptionURL}}
{{end}}{{if .Access.ClientHint}}
{{.Access.ClientHint}}
{{end}}
Используйте эти обновлённые данные вместо старых копий и храните их в секрете.{{end}}
{{define "html"}}<p>Ваш доступ к RouteGate VPN был обновлён.</p>{{if .ProfileName}}<p>Профиль: <strong>{{.ProfileName}}</strong></p>{{end}}<p>Протокол: <strong>{{.Access.DisplayName}}</strong></p><p>{{.Access.PrimaryAction}}</p>{{if .Access.URI}}<p>Обновлённые данные подключения:<br><code style="word-break:break-all">{{.Access.URI}}</code></p>{{end}}{{if .Access.AlternativeURI}}<p><a href="{{.Access.AlternativeURI}}">Открыть обновлённый прокси в Telegram</a></p>{{end}}{{if .Access.ConfigText}}<p>Обновлённая конфигурация WireGuard:</p><pre style="white-space:pre-wrap;word-break:break-all;background:#f3f4f6;padding:12px;border-radius:8px">{{.Access.ConfigText}}</pre>{{end}}{{if .Access.SubscriptionURL}}<p><a href="{{.Access.SubscriptionURL}}">Открыть подписку</a></p>{{end}}{{if .Access.ClientHint}}<p>{{.Access.ClientHint}}</p>{{end}}<p>Используйте эти обновлённые данные вместо старых копий и храните их в секрете.</p>{{end}}
